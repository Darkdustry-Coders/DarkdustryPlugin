package pandorum.listeners.events;

import arc.func.Cons;
import mindustry.game.EventType.WaveEvent;

public class OnWave implements Cons<WaveEvent> {

    public void get(WaveEvent event) {
        // Пока что делает ничего
    }
}
